export const bindEvents = (el, eventName, callback) => {
  if (!el) return
  return el.addEventListener(eventName, callback, false)
}
export const unBindEvents = (el, eventName, callback) => {
  if (!el) return
  el.removeEventListener(eventName, callback, false)
}
