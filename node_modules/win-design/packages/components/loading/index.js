import directive from './src/directive'
import service from './src/index'

export default {
  // 单组件单注册
  install (Vue) {
    Vue.use(directive)
    Vue.prototype.$loading = service
  },
  directive,
  service
}
