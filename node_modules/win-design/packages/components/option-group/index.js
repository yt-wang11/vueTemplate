import WOptionGroup from '../select/src/option-group'

/* istanbul ignore next */
WOptionGroup.install = function (Vue) {
  Vue.component(WOptionGroup.name, WOptionGroup)
}

export default WOptionGroup
