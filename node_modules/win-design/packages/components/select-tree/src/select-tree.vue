<template>
  <div
    class="w-select"
    :class="[selectSize ? 'w-select--' + selectSize : '']"
    @click.stop="toggleMenu"
    v-clickoutside="handleClose">
    <div
      class="w-select__tags"
      v-if="multiple"
      ref="tags"
      :style="{ 'max-width': inputWidth - 32 + 'px', width: '100%' }">
      <transition-group @after-leave="resetInputHeight" v-if="!collapseTags">
        <w-tag
          v-for="item in selected"
          :key="getValueKey(item)"
          :closable="!selectDisabled"
          :size="collapseTagSize"
          :hit="item.hitState"
          @close="deleteTag(item)"
          disable-transitions>
          <span class="w-select__tags-text">{{ getValueOfSelectedItem(item) }}</span>
        </w-tag>
      </transition-group>
    </div>
    <w-input
      ref="reference"
      v-model="selectedLabel"
      type="text"
      :placeholder="currentPlaceholder"
      :name="name"
      :id="id"
      :autocomplete="autoComplete || autocomplete"
      :size="selectSize"
      :disabled="selectDisabled"
      :readonly="readonly"
      :validate-event="false"
      :class="{ 'is-focus': visible }"
      :suffix-icon="`w-icon-${iconClass}`"
      :clearable="showClose"
      @focus="handleFocus"
      @blur="handleBlur"
      @keyup.native="debouncedOnInputChange"
      @keydown.native.down.stop.prevent="navigateOptions('next')"
      @keydown.native.up.stop.prevent="navigateOptions('prev')"
      @keydown.native.esc.stop.prevent="visible = false"
      @keydown.native.tab="visible = false"
      @paste.native="debouncedOnInputChange"
      @mouseenter.native="inputHovering = true"
      @mouseleave.native="inputHovering = false">
    </w-input>
    <transition
      name="w-zoom-in-top"
      @before-enter="handleMenuEnter"
      @after-leave="destroyPopper">
      <w-select-tree-menu
        ref="popper"
        :append-to-body="popperAppendToBody"
        v-show="visible && emptyText !== false">
        <w-scrollbar
          wrap-class="w-select-dropdown__wrap"
          view-class="w-select-dropdown__list"
          ref="scrollbar"
          :class="{ 'is-empty': !allowCreate && query && filteredOptionsCount === 0 }"
          v-show="!loading">
          <w-tree
            :data="data"
            :props="props"
            :show-checkbox="multiple"
            @check="handleCheckChange"
            @node-click="handleNodeClick"
            :render-content="renderContent"
            :node-key="nodeKey"
            ref="tree"
          ></w-tree>
        </w-scrollbar>
        <p
          class="w-select-dropdown__empty"
          v-if="emptyText && !loading && !data.length">
          {{ emptyText }}
        </p>
      </w-select-tree-menu>
    </transition>
  </div>
</template>

<script type="text/babel">
import Emitter from '../../../mixins/emitter'
import WInput from '../../input'
import WSelectTreeMenu from '../../select-menu'
import WTag from '../../tag'
import WScrollbar from '../../scrollbar'
import WTree from '../../tree'
import {debounce} from 'throttle-debounce'
import Clickoutside from '../../../utils/clickoutside'
import {addResizeListener, removeResizeListener} from '../../../utils/resize-event'
import scrollIntoView from '../../../utils/scroll-into-view'
import {getValueByPath, valueEquals} from '../../../utils/utils'
import NavigationMixin from '../../select/src/navigation-mixin'

export default {
  name: 'WSelectTree',

  props: {
    name: String,
    id: String,
    data: {
      type: Array
    },
    props: {
      default () {
        return {
          children: 'children',
          label: 'label',
          disabled: 'disabled'
        }
      }
    },
    value: {
      required: true
    },
    nodeKey: {
      type: String,
      default: 'id'
    },
    nodeLabel: {
      type: String,
      default: 'label'
    },
    autocomplete: {
      type: String,
      default: 'off'
    },
    /** @Deprecated in next major version */
    autoComplete: {
      type: String,
      validator (val) {
        process.env.NODE_ENV !== 'production' &&
        console.warn('[Win-Design][Select]\'auto-complete\' property will be deprecated in next major version. please use \'autocomplete\' instead.')
        return true
      }
    },
    automaticDropdown: Boolean,
    size: {
      type: String,
      default: 'medium'
    },
    disabled: Boolean,
    clearable: {
      type: Boolean,
      default: false
    },
    filterable: Boolean,
    allowCreate: Boolean,
    loading: Boolean,
    popperClass: String,
    remote: Boolean,
    loadingText: String,
    noMatchText: String,
    noDataText: String,
    remoteMethod: Function,
    filterMethod: Function,
    multiple: Boolean,
    multipleLimit: {
      type: Number,
      default: 0
    },
    placeholder: {
      type: String,
      default () {
        return '请选择'
      }
    },
    defaultFirstOption: Boolean,
    reserveKeyword: Boolean,
    valueKey: {
      type: String,
      default: 'value'
    },
    collapseTags: Boolean,
    popperAppendToBody: {
      type: Boolean,
      default: true
    }
  },

  data () {
    return {
      options: [],
      cachedOptions: [],
      createdLabel: null,
      createdSelected: false,
      selected: this.multiple ? [] : {},
      inputLength: 20,
      inputWidth: 0,
      initialInputHeight: 0,
      cachedPlaceHolder: '',
      optionsCount: 0,
      filteredOptionsCount: 0,
      visible: false,
      softFocus: false,
      selectedLabel: '',
      hoverIndex: -1,
      query: '',
      previousQuery: null,
      inputHovering: false,
      currentPlaceholder: '',
      menuVisibleOnFocus: false,
      isOnComposition: false,
      isSilentBlur: false,
      deletedTag: false
    }
  },

  watch: {
    selectDisabled () {
      this.$nextTick(() => {
        this.resetInputHeight()
      })
    },

    placeholder (val) {
      this.cachedPlaceHolder = this.currentPlaceholder = val
    },

    value (val, oldVal) {
      if (this.multiple) {
        this.resetInputHeight()
        if (val.length > 0 || (this.$refs.input && this.query !== '')) {
          this.currentPlaceholder = ''
        } else {
          this.currentPlaceholder = this.cachedPlaceHolder
        }
        if (this.filterable && !this.reserveKeyword) {
          this.query = ''
          this.handleQueryChange(this.query)
        }
      }
      this.setSelected()
      if (this.filterable && !this.multiple) {
        this.inputLength = 20
      }
      if (!valueEquals(val, oldVal)) {
        this.dispatchEvent('WFormItem', 'w.form.field.change', val)
      }
    },

    visible (val) {
      if (!val) {
        this.broadcast('WSelectDropdown', 'destroyPopper')
        if (this.$refs.input) {
          this.$refs.input.blur()
        }
        this.query = ''
        this.previousQuery = null
        this.selectedLabel = ''
        this.inputLength = 20
        this.menuVisibleOnFocus = false
        this.resetHoverIndex()
        this.$nextTick(() => {
          if (this.$refs.input &&
              this.$refs.input.value === '' &&
              this.selected.length === 0) {
            this.currentPlaceholder = this.cachedPlaceHolder
          }
        })
        if (!this.multiple) {
          if (this.selected) {
            if (this.filterable && this.allowCreate &&
                this.createdSelected && this.createdLabel) {
              this.selectedLabel = this.createdLabel
            } else {
              this.selectedLabel = this.selected[this.props.label]
            }
            if (this.filterable) this.query = this.selectedLabel
          }
        }
      } else {
        this.broadcast('WSelectDropdown', 'updatePopper')
        if (this.filterable) {
          this.query = this.remote ? '' : this.selectedLabel
          this.handleQueryChange(this.query)
          if (this.multiple) {
          } else {
            if (!this.remote) {
              this.broadcast('WOption', 'queryChange', '')
              this.broadcast('WOptionGroup', 'queryChange')
            }
            this.broadcast('WInput', 'inputSelect')
          }
        }
      }
      this.$emit('visible-change', val)
    },

    options () {
      if (this.$isServer) return
      this.$nextTick(() => {
        this.broadcast('WSelectTreeDropdown', 'updatePopper')
      })
      if (this.multiple) {
        this.resetInputHeight()
      }
      let inputs = this.$el.querySelectorAll('input')
      if ([].indexOf.call(inputs, document.activeElement) === -1) {
        this.setSelected()
      }
      if (this.defaultFirstOption && (this.filterable || this.remote) && this.filteredOptionsCount) {
        this.checkDefaultFirstOption()
      }
    }
  },

  componentName: 'WSelectTree',

  inject: {
    wForm: {
      default: ''
    },

    wFormItem: {
      default: ''
    }
  },

  provide () {
    return {
      'select': this
    }
  },

  computed: {
    _wFormItemSize () {
      return (this.wFormItem || {}).wFormItemSize
    },

    readonly () {
      return !this.filterable || this.multiple || !this.visible
    },

    showClose () {
      let hasValue = this.multiple
        ? Array.isArray(this.value) && this.value.length > 0
        : this.value !== undefined && this.value !== null && this.value !== ''
      let criteria = this.clearable &&
          !this.selectDisabled &&
          this.inputHovering &&
          hasValue
      return criteria
    },

    iconClass () {
      return this.remote && this.filterable ? '' : (this.visible ? 'triangle-top' : 'triangle-bottom')
    },

    debounce () {
      return this.remote ? 300 : 0
    },

    emptyText () {
      if (this.loading) {
        return this.loadingText || '加载中'
      } else {
        if (this.remote && this.query === '' && this.options.length === 0) return false
        if (this.filterable && this.query && this.options.length > 0 && this.filteredOptionsCount === 0) {
          return this.noMatchText || '无匹配数据'
        }
        if (this.options.length === 0) {
          return this.noDataText || '无数据'
        }
      }
      return null
    },

    showNewOption () {
      let hasExistingOption = this.options.filter(option => !option.created)
        .some(option => option.currentLabel === this.query)
      return this.filterable && this.allowCreate && this.query !== '' && !hasExistingOption
    },

    selectSize () {
      return this.size || this._wFormItemSize || (this.$WINDESIGN || {}).size
    },

    selectDisabled () {
      return this.disabled || (this.wForm || {}).disabled
    },

    collapseTagSize () {
      return ['small', 'mini'].indexOf(this.selectSize) > -1
        ? 'mini'
        : 'small'
    }
  },

  components: {
    WInput,
    WSelectTreeMenu,
    WTag,
    WScrollbar,
    WTree
  },

  directives: {Clickoutside},

  methods: {
    close () {
      this.visible = false
    },
    getLabel (data) {
      return data[this.props.label]
    },
    handleCheckChange (data, node, vm) {
      this.selected = node.checkedNodes
      this.resetInputHeight()
      this.$emit('input', this.selected)
    },
    handleNodeClick (data, node, element) {
      if (this.multiple) {
        return false
      }
      this.selected = data
      this.$emit('input', this.selected)
    },
    renderContent (h, {node, data, store}) {
      let showICon
      let isSelected
      if (!this.multiple && this.selected === data) {
        showICon = <i class="w-icon-right"></i>
      }
      if (!this.multiple && this.selected === data) {
        isSelected = true
      }
      if (this.multiple && this.selected.includes(data)) {
        isSelected = true
      }
      return (
        <span class={{'is-actived': isSelected, 'w-tree-node__label': true}}>
          <span class="w-tree-node__label__name">
            <span>{node.label}</span>
          </span>
          <span class="w-tree-node__label__icon">
            {showICon}
          </span>
        </span>)
    },
    handleComposition (event) {
      const text = event.target.value
      if (event.type === 'compositionend') {
        this.isOnComposition = false
        this.handleQueryChange(text)
      }
      this.isOnComposition = true
    },
    handleQueryChange (val) {
      if (this.previousQuery === val || this.isOnComposition) return
      if (
        this.previousQuery === null &&
          (typeof this.filterMethod === 'function' || typeof this.remoteMethod === 'function')
      ) {
        this.previousQuery = val
        return
      }
      this.previousQuery = val
      this.$nextTick(() => {
        if (this.visible) this.broadcast('WSelectDropdown', 'updatePopper')
      })
      this.hoverIndex = -1
      if (this.multiple && this.filterable) {
        const length = this.$refs.input.value.length * 15 + 20
        this.inputLength = this.collapseTags ? Math.min(50, length) : length
        this.managePlaceholder()
        this.resetInputHeight()
      }
      if (this.remote && typeof this.remoteMethod === 'function') {
        this.hoverIndex = -1
        this.remoteMethod(val)
      } else if (typeof this.filterMethod === 'function') {
        this.filterMethod(val)
        this.broadcast('WOptionGroup', 'queryChange')
      } else {
        this.filteredOptionsCount = this.optionsCount
        this.broadcast('WOption', 'queryChange', val)
        this.broadcast('WOptionGroup', 'queryChange')
      }
      if (this.defaultFirstOption && (this.filterable || this.remote) && this.filteredOptionsCount) {
        this.checkDefaultFirstOption()
      }
    },

    scrollToOption (option) {
      const target = Array.isArray(option) && option[0] ? option[0].$el : option.$el
      if (this.$refs.popper && target) {
        const menu = this.$refs.popper.$el.querySelector('.w-select-dropdown__wrap')
        scrollIntoView(menu, target)
      }
      this.$refs.scrollbar && this.$refs.scrollbar.handleScroll()
    },

    handleMenuEnter () {
      // this.$nextTick(() => this.scrollToOption(this.selected))
    },

    emitChange (val) {
      if (!valueEquals(this.value, val)) {
        this.$emit('change', val)
      }
    },

    getOption (value) {
      let option
      const isObject = Object.prototype.toString.call(value).toLowerCase() === '[object object]'
      const isNull = Object.prototype.toString.call(value).toLowerCase() === '[object null]'

      for (let i = this.cachedOptions.length - 1; i >= 0; i--) {
        const cachedOption = this.cachedOptions[i]
        const isEqual = isObject
          ? getValueByPath(cachedOption.value, this.valueKey) === getValueByPath(value, this.valueKey)
          : cachedOption.value === value
        if (isEqual) {
          option = cachedOption
          break
        }
      }
      if (option) return option
      const label = (!isObject && !isNull)
        ? value : ''
      let newOption = {
        value: value,
        currentLabel: label
      }
      if (this.multiple) {
        newOption.hitState = false
      }
      return newOption
    },

    setSelected () {
      if (!this.multiple) {
        this.selectedLabel = this.getLabel(this.selected)
        if (this.filterable) this.query = this.selectedLabel
        return
      }
      this.selected = this.value
      this.$nextTick(() => {
        this.resetInputHeight()
      })
    },

    handleFocus (event) {
      if (!this.softFocus) {
        if (this.automaticDropdown || this.filterable) {
          this.visible = true
          this.menuVisibleOnFocus = true
        }
        this.$emit('focus', event)
      } else {
        this.softFocus = false
      }
    },

    blur () {
      this.visible = false
      this.$refs.reference.blur()
    },

    handleBlur (event) {
      setTimeout(() => {
        if (this.isSilentBlur) {
          this.isSilentBlur = false
        } else {
          this.$emit('blur', event)
        }
      }, 50)
      this.softFocus = false
    },

    handleClearClick (event) {
      this.deleteSelected(event)
    },

    destroyPopper () {
      this.$refs.popper && this.$refs.popper.destroyPopper()
    },

    handleClose () {
      this.visible = false
    },

    toggleLastOptionHitState (hit) {
      if (!Array.isArray(this.selected)) return
      const option = this.selected[this.selected.length - 1]
      if (!option) return

      if (hit === true || hit === false) {
        option.hitState = hit
        return hit
      }

      option.hitState = !option.hitState
      return option.hitState
    },

    deletePrevTag (e) {
      if (e.target.value.length <= 0 && !this.toggleLastOptionHitState()) {
        const value = this.value.slice()
        value.pop()
        this.$emit('input', value)
        this.emitChange(value)
      }
    },

    managePlaceholder () {
      if (this.currentPlaceholder !== '') {
        this.currentPlaceholder = this.$refs.input.value ? '' : this.cachedPlaceHolder
      }
    },

    resetInputState (e) {
      if (e.keyCode !== 8) this.toggleLastOptionHitState(false)
      this.inputLength = this.$refs.input.value.length * 15 + 20
      this.resetInputHeight()
    },

    resetInputHeight () {
      if (this.collapseTags && !this.filterable) return
      this.$nextTick(() => {
        if (!this.$refs.reference) return
        let inputChildNodes = this.$refs.reference
        let input = inputChildNodes.$refs.input
        const tags = this.$refs.tags
        const sizeInMap = this.initialInputHeight || 40
        input.style.height = this.selected.length === 0
          ? sizeInMap + 'px'
          : Math.max(tags ? (tags.clientHeight + (tags.clientHeight > sizeInMap ? 6 : 0)) : 0, sizeInMap) + 'px'
        if (this.visible && this.emptyText !== false) {
          this.broadcast('WSelectDropdown', 'updatePopper')
        }
      })
    },

    resetHoverIndex () {
      setTimeout(() => {
        if (!this.multiple) {
          this.hoverIndex = this.options.indexOf(this.selected)
        } else {
          if (this.selected.length > 0) {
            this.hoverIndex = Math.min.apply(null, this.selected.map(item => this.options.indexOf(item)))
          } else {
            this.hoverIndex = -1
          }
        }
      }, 300)
    },

    handleNodeSelect (data, isChecked) {
      if (this.multiple) {
        const index = this.selected.indexOf(data)
        if (isChecked && !~index) {
          this.selected.push(data)
        } else if (!isChecked && ~index) {
          this.selected.splice(index, 1)
        }
      } else {
        // TODO: 单选
      }

      if (this.visible) return
      this.$nextTick(() => {
        this.scrollToOption(data)
      })
    },
    getValueIndex (arr = [], value) {
      const isObject = Object.prototype.toString.call(value).toLowerCase() === '[object object]'
      if (!isObject) {
        return arr.indexOf(value)
      } else {
        const valueKey = this.valueKey
        let index = -1
        arr.some((item, i) => {
          if (getValueByPath(item, valueKey) === getValueByPath(value, valueKey)) {
            index = i
            return true
          }
          return false
        })
        return index
      }
    },

    toggleMenu () {
      if (this.deletedTag) {
        this.deletedTag = false
        return
      }
      if (!this.selectDisabled) {
        if (this.menuVisibleOnFocus) {
          this.menuVisibleOnFocus = false
        } else {
          this.visible = !this.visible
        }
      }
    },

    selectOption () {
      if (!this.visible) {
        this.toggleMenu()
      }
    },

    deleteSelected () {
      const value = this.multiple ? [] : ''
      this.$emit('input', value)
      this.emitChange(value)
      this.visible = false
      this.$emit('clear')
    },

    async deleteTag (tag) {
      this.deletedTag = true
      const tree = this.$refs.tree
      tree.setChecked(tag, false, true)

      for (let i = 0; i < this.selected.length; i++) {
        const item = this.selected[i]
        const node = await tree.getNode(item)

        if (!node.checked) {
          const data = node.data
          const index = this.selected.indexOf(data)
          if (~index) {
            this.selected.splice(index, 1)
            this.$emit('remove-tag', data)
            i--
          }
        }
      }
      const value = this.selected.slice()
      this.$emit('input', value)
      this.emitChange(value)
    },

    onInputChange () {
      if (this.filterable && this.query !== this.selectedLabel) {
        this.query = this.selectedLabel
        this.handleQueryChange(this.query)
      }
    },

    onOptionDestroy (index) {
      if (index > -1) {
        this.optionsCount--
        this.filteredOptionsCount--
        this.options.splice(index, 1)
      }
    },

    resetInputWidth () {
      this.inputWidth = this.$refs.reference.$el.getBoundingClientRect().width
    },

    handleResize () {
      this.resetInputWidth()
      if (this.multiple) this.resetInputHeight()
    },

    checkDefaultFirstOption () {
      this.hoverIndex = -1
      // highlight the created option
      let hasCreated = false
      for (let i = this.options.length - 1; i >= 0; i--) {
        if (this.options[i].created) {
          hasCreated = true
          this.hoverIndex = i
          break
        }
      }
      if (hasCreated) return
      for (let i = 0; i !== this.options.length; ++i) {
        const option = this.options[i]
        if (this.query) {
          // highlight first options that passes the filter
          if (!option.disabled && !option.groupDisabled && option.visible) {
            this.hoverIndex = i
            break
          }
        } else {
          // highlight currently selected option
          if (option.itemSelected) {
            this.hoverIndex = i
            break
          }
        }
      }
    },

    getValueKey (item) {
      return getValueByPath(item, this.nodeKey)
    },

    getValueOfSelectedItem (item) {
      return getValueByPath(item, this.nodeLabel)
    }
  },

  mixins: [Emitter, NavigationMixin],

  created () {
    this.cachedPlaceHolder = this.currentPlaceholder = this.placeholder
    if (this.multiple && !Array.isArray(this.value)) {
      this.$emit('input', [])
    }
    if (!this.multiple && Array.isArray(this.value)) {
      this.$emit('input', '')
    }

    this.debouncedOnInputChange = debounce(this.debounce, () => {
      this.onInputChange()
    })

    this.debouncedQueryChange = debounce(this.debounce, (e) => {
      this.handleQueryChange(e.target.value)
    })

    this.$on('setSelected', this.setSelected)
  },

  mounted () {
    if (this.multiple && Array.isArray(this.value) && this.value.length > 0) {
      this.currentPlaceholder = ''
    }
    addResizeListener(this.$el, this.handleResize)

    const reference = this.$refs.reference
    if (reference && reference.$el) {
      const sizeMap = {
        medium: 36,
        small: 32,
        mini: 28
      }
      this.initialInputHeight = reference.$el.getBoundingClientRect().height || sizeMap[this.selectSize]
    }
    if (this.remote && this.multiple) {
      this.resetInputHeight()
    }
    this.$nextTick(() => {
      if (reference && reference.$el) {
        this.inputWidth = reference.$el.getBoundingClientRect().width
      }
    })
    this.setSelected()
  },

  beforeDestroy () {
    if (this.$el && this.handleResize) removeResizeListener(this.$el, this.handleResize)
  }
}
</script>
